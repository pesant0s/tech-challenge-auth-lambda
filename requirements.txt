# Dependências empacotadas junto com a função.
# Ambas são Python puro — não há binário compilado, então o pacote é montado
# em qualquer sistema operacional com um `pip install -t`.
pg8000==1.31.2
PyJWT==2.9.0
